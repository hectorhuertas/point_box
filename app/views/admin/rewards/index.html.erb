<%= link_to "New Reward", new_admin_reward_path %>
<% @rewards.each do |reward| %>
<div id="reward-<%= reward.id %>">
  <%= reward.name %>: <%= pluralize reward.cost, 'point' %>
  <%= image_tag reward.picture.url %>
</div>
<% end %>